version: "3.9"

services:
  dubai-kunafa-chocolate:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dubai-kunafa-chocolate
    ports:
      - "8080:80"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:80/"]
      interval: 30s
      timeout: 3s
      start_period: 5s
      retries: 3
